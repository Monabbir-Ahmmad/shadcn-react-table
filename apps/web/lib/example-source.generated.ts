// AUTO-GENERATED by apps/web/scripts/build-example-source.mjs — do not edit by hand.
// Regenerate with `pnpm --filter shadcn-react-table-web examples:build`.

export const EXAMPLE_SOURCE: Record<string, string> = {
  "basic": "function BasicExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "sorting": "function SortingExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableMultiSort: true,\n    initialState: {\n      sorting: [\n        { id: \"department\", desc: false },\n        { id: \"salary\", desc: true },\n      ],\n      pagination: { pageSize: 10 },\n    },\n  })\n  return <DataTable table={table} />\n}",
  "column-filters": "function ColumnFiltersExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    defaultShowColumnFilters: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "filter-modes": "function FilterModesExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    defaultShowColumnFilters: true,\n    enableColumnFilterModes: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "global-search": "function GlobalSearchExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableGlobalFilter: true,\n    defaultGlobalFilterMode: \"fuzzy\",\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "advanced-filter": "function AdvancedFilterExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableAdvancedFilter: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "column-ordering": "function ColumnOrderingExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableColumnOrdering: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "column-pinning": "function ColumnPinningExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableColumnPinning: true,\n    initialState: {\n      columnPinning: { left: [\"firstName\"], right: [\"salary\"] },\n      pagination: { pageSize: 10 },\n    },\n  })\n  return <DataTable table={table} />\n}",
  "column-resizing": "function ColumnResizingExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableColumnResizing: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "column-visibility": "function ColumnVisibilityExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    initialState: {\n      columnVisibility: { startDate: false, progress: false },\n      pagination: { pageSize: 10 },\n    },\n  })\n  return <DataTable table={table} />\n}",
  "density": "function DensityExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    defaultDensity: \"compact\",\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "sticky": "function StickyExample() {\n  const data = React.useMemo(() => makeUsers(40), [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enablePagination: false,\n    enableStickyFooter: true,\n  })\n  return <DataTable table={table} surfaceClassName=\"max-h-[460px]\" />\n}",
  "loading": "function LoadingExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const [isLoading, setIsLoading] = React.useState(true)\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    isLoading,\n    initialState: { pagination: { pageSize: 10 } },\n    renderToolbarActions: () => (\n      <Button\n        variant=\"outline\"\n        size=\"sm\"\n        onClick={() => setIsLoading((v) => !v)}\n      >\n        {isLoading ? \"Show data\" : \"Show loading\"}\n      </Button>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "row-selection": "function RowSelectionExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableRowSelection: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "single-selection": "function SingleSelectionExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableRowSelection: true,\n    enableMultiRowSelection: false,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "row-numbers": "function RowNumbersExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableRowNumbers: true,\n    rowNumberMode: \"static\",\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "row-pinning": "function RowPinningExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableRowNumbers: true,\n    enableRowPinning: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "row-ordering": "function RowOrderingExample() {\n  const [data, setData] = React.useState(() => makeUsers(12))\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableRowOrdering: true,\n    enablePagination: false,\n    onRowOrderChange: (activeId, overId) =>\n      setData((prev) => {\n        const from = prev.findIndex((r) => r.id === activeId)\n        const to = prev.findIndex((r) => r.id === overId)\n        if (from === -1 || to === -1) return prev\n        const next = [...prev]\n        const [moved] = next.splice(from, 1)\n        if (moved) next.splice(to, 0, moved)\n        return next\n      }),\n  })\n  return <DataTable table={table} />\n}",
  "grouping": "function GroupingExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableGrouping: true,\n    enableStickyFooter: true,\n    initialState: {\n      grouping: [\"department\"],\n      expanded: true,\n      pagination: { pageSize: 50 },\n    },\n  })\n  return <DataTable table={table} />\n}",
  "detail-panel": "function DetailPanelExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    initialState: { pagination: { pageSize: 10 } },\n    renderDetailPanel: ({ row }) => (\n      <dl className=\"grid grid-cols-2 gap-3 text-xs sm:grid-cols-4\">\n        <Field label=\"City\">{row.original.city}</Field>\n        <Field label=\"Country\">{row.original.country}</Field>\n        <Field label=\"Start date\">\n          {row.original.startDate.toLocaleDateString()}\n        </Field>\n        <Field label=\"Salary\">{currency.format(row.original.salary)}</Field>\n      </dl>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "tree": "function TreeExample() {\n  const data = React.useMemo(() => orgData, [])\n  const columns = React.useMemo(() => orgColumns, [])\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    getSubRows: (row) => row.subRows,\n    enablePagination: false,\n    initialState: { expanded: true },\n  })\n  return <DataTable table={table} />\n}",
  "editing-cell": "// <EditingExample mode=\"cell\" />\nfunction EditingExample({ mode }: { mode: EditDisplayMode }) {\n  const [data, setData] = React.useState(() => makeUsers(12))\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableEditing: true,\n    editDisplayMode: mode,\n    enablePagination: false,\n    createRowDefaults: {\n      firstName: \"\",\n      lastName: \"\",\n      email: \"\",\n      role: \"Viewer\",\n      department: \"Engineering\",\n      status: \"pending\",\n      age: 30,\n      salary: 50000,\n    },\n    onEditCellSave: ({ row, column, value }) =>\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? ({ ...r, [column.id]: value } as User) : r\n        )\n      ),\n    onSaveRow: ({ row, values, exit }) => {\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? { ...r, ...(values as Partial<User>) } : r\n        )\n      )\n      exit()\n    },\n    onCreateRow: ({ values, exit }) => {\n      setData((prev) => [\n        {\n          ...makeUsers(1)[0]!,\n          id: `U-${2000 + prev.length}`,\n          ...(values as Partial<User>),\n        },\n        ...prev,\n      ])\n      exit()\n    },\n    renderToolbarActions: ({ table }) => (\n      <Button\n        variant=\"outline\"\n        size=\"sm\"\n        onClick={() => table.tableInstance.beginCreate()}\n      >\n        <RiAddLine />\n        New\n      </Button>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "editing-row": "// <EditingExample mode=\"row\" />\nfunction EditingExample({ mode }: { mode: EditDisplayMode }) {\n  const [data, setData] = React.useState(() => makeUsers(12))\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableEditing: true,\n    editDisplayMode: mode,\n    enablePagination: false,\n    createRowDefaults: {\n      firstName: \"\",\n      lastName: \"\",\n      email: \"\",\n      role: \"Viewer\",\n      department: \"Engineering\",\n      status: \"pending\",\n      age: 30,\n      salary: 50000,\n    },\n    onEditCellSave: ({ row, column, value }) =>\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? ({ ...r, [column.id]: value } as User) : r\n        )\n      ),\n    onSaveRow: ({ row, values, exit }) => {\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? { ...r, ...(values as Partial<User>) } : r\n        )\n      )\n      exit()\n    },\n    onCreateRow: ({ values, exit }) => {\n      setData((prev) => [\n        {\n          ...makeUsers(1)[0]!,\n          id: `U-${2000 + prev.length}`,\n          ...(values as Partial<User>),\n        },\n        ...prev,\n      ])\n      exit()\n    },\n    renderToolbarActions: ({ table }) => (\n      <Button\n        variant=\"outline\"\n        size=\"sm\"\n        onClick={() => table.tableInstance.beginCreate()}\n      >\n        <RiAddLine />\n        New\n      </Button>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "editing-table": "// <EditingExample mode=\"table\" />\nfunction EditingExample({ mode }: { mode: EditDisplayMode }) {\n  const [data, setData] = React.useState(() => makeUsers(12))\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableEditing: true,\n    editDisplayMode: mode,\n    enablePagination: false,\n    createRowDefaults: {\n      firstName: \"\",\n      lastName: \"\",\n      email: \"\",\n      role: \"Viewer\",\n      department: \"Engineering\",\n      status: \"pending\",\n      age: 30,\n      salary: 50000,\n    },\n    onEditCellSave: ({ row, column, value }) =>\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? ({ ...r, [column.id]: value } as User) : r\n        )\n      ),\n    onSaveRow: ({ row, values, exit }) => {\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? { ...r, ...(values as Partial<User>) } : r\n        )\n      )\n      exit()\n    },\n    onCreateRow: ({ values, exit }) => {\n      setData((prev) => [\n        {\n          ...makeUsers(1)[0]!,\n          id: `U-${2000 + prev.length}`,\n          ...(values as Partial<User>),\n        },\n        ...prev,\n      ])\n      exit()\n    },\n    renderToolbarActions: ({ table }) => (\n      <Button\n        variant=\"outline\"\n        size=\"sm\"\n        onClick={() => table.tableInstance.beginCreate()}\n      >\n        <RiAddLine />\n        New\n      </Button>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "editing-modal": "// <EditingExample mode=\"modal\" />\nfunction EditingExample({ mode }: { mode: EditDisplayMode }) {\n  const [data, setData] = React.useState(() => makeUsers(12))\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableEditing: true,\n    editDisplayMode: mode,\n    enablePagination: false,\n    createRowDefaults: {\n      firstName: \"\",\n      lastName: \"\",\n      email: \"\",\n      role: \"Viewer\",\n      department: \"Engineering\",\n      status: \"pending\",\n      age: 30,\n      salary: 50000,\n    },\n    onEditCellSave: ({ row, column, value }) =>\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? ({ ...r, [column.id]: value } as User) : r\n        )\n      ),\n    onSaveRow: ({ row, values, exit }) => {\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? { ...r, ...(values as Partial<User>) } : r\n        )\n      )\n      exit()\n    },\n    onCreateRow: ({ values, exit }) => {\n      setData((prev) => [\n        {\n          ...makeUsers(1)[0]!,\n          id: `U-${2000 + prev.length}`,\n          ...(values as Partial<User>),\n        },\n        ...prev,\n      ])\n      exit()\n    },\n    renderToolbarActions: ({ table }) => (\n      <Button\n        variant=\"outline\"\n        size=\"sm\"\n        onClick={() => table.tableInstance.beginCreate()}\n      >\n        <RiAddLine />\n        New\n      </Button>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "row-actions": "function RowActionsExample() {\n  const [data, setData] = React.useState(() => baseUsers)\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    initialState: { pagination: { pageSize: 10 } },\n    renderRowActions: ({ row }) => (\n      <Button\n        variant=\"ghost\"\n        size=\"icon-xs\"\n        aria-label=\"Delete row\"\n        onClick={() =>\n          setData((prev) => prev.filter((r) => r.id !== row.original.id))\n        }\n      >\n        <RiDeleteBinLine />\n      </Button>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "cell-actions": "function CellActionsExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableClickToCopy: true,\n    initialState: { pagination: { pageSize: 10 } },\n    renderCellActionMenuItems: ({ cell }) => (\n      <>\n        <ContextMenuItem\n          onClick={() =>\n            navigator.clipboard?.writeText(String(cell.getValue() ?? \"\"))\n          }\n        >\n          Copy value\n        </ContextMenuItem>\n        <ContextMenuItem\n          onClick={() =>\n            navigator.clipboard?.writeText(JSON.stringify(cell.row.original))\n          }\n        >\n          Copy row as JSON\n        </ContextMenuItem>\n      </>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "virtualized": "function VirtualizedExample() {\n  const data = React.useMemo(() => makeUsers(1000), [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enablePagination: false,\n    enableRowVirtualization: true,\n  })\n  return <DataTable table={table} />\n}",
  "column-virtualization": "function ColumnVirtualizationExample() {\n  const data = React.useMemo(() => makeUsers(300), [])\n  const columns = React.useMemo(() => wideColumns(), [])\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enablePagination: false,\n    enableRowVirtualization: true,\n    enableColumnVirtualization: true,\n  })\n  return <DataTable table={table} surfaceClassName=\"max-h-[600px]\" />\n}",
  "event-listeners": "function EventListenersExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const [last, setLast] = React.useState<string | null>(null)\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    initialState: { pagination: { pageSize: 10 } },\n    onRowClick: ({ row }) => setLast(row.original.email),\n    renderToolbarActions: () => (\n      <span className=\"text-xs text-muted-foreground\">\n        {last ? `Last clicked: ${last}` : \"Click any row\"}\n      </span>\n    ),\n  })\n  return <DataTable table={table} />\n}",
  "custom-icons": "function CustomIconsExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    defaultShowColumnFilters: true,\n    icons: {\n      sortAscending: RiSortAsc,\n      sortDescending: RiSortDesc,\n      columnActions: RiMoreFill,\n      columnVisibility: RiEqualizerLine,\n    },\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "export": "function ExportExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableRowSelection: true,\n    enableExport: true,\n    exportFileName: \"users\",\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "localization": "function LocalizationExample() {\n  const data = React.useMemo(() => baseUsers, [])\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    localization: SPANISH,\n    defaultShowColumnFilters: true,\n    initialState: { pagination: { pageSize: 10 } },\n  })\n  return <DataTable table={table} />\n}",
  "server-side": "function ServerSideExample() {\n  const all = React.useMemo(() => makeUsers(95), [])\n  const [pageIndex, setPageIndex] = React.useState(0)\n  const [isLoading, setIsLoading] = React.useState(false)\n\n  // Simulate a paged server response with a short delay.\n  const [page, setPage] = React.useState(() => all.slice(0, PAGE_SIZE))\n  React.useEffect(() => {\n    // Simulated server fetch: flag loading, then resolve the page after a delay.\n    // eslint-disable-next-line react-hooks/set-state-in-effect\n    setIsLoading(true)\n    const id = setTimeout(() => {\n      setPage(all.slice(pageIndex * PAGE_SIZE, (pageIndex + 1) * PAGE_SIZE))\n      setIsLoading(false)\n    }, 400)\n    return () => clearTimeout(id)\n  }, [all, pageIndex])\n\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data: page,\n    columns,\n    getRowId: (row) => row.id,\n    isLoading,\n    manualPagination: true,\n    rowCount: all.length,\n    state: { pagination: { pageIndex, pageSize: PAGE_SIZE } },\n    onPaginationChange: (updater) => {\n      const next =\n        typeof updater === \"function\"\n          ? updater({ pageIndex, pageSize: PAGE_SIZE })\n          : updater\n      setPageIndex(next.pageIndex)\n    },\n  })\n  return <DataTable table={table} />\n}",
  "advanced": "function AdvancedExample() {\n  const [data, setData] = React.useState(() => baseUsers)\n  const columns = useUserColumns()\n  const table = useDataTable({\n    data,\n    columns,\n    getRowId: (row) => row.id,\n    enableRowSelection: true,\n    enableMultiSort: true,\n    enableColumnOrdering: true,\n    enableColumnPinning: true,\n    enableColumnResizing: true,\n    enableRowNumbers: true,\n    enableRowPinning: true,\n    enableGrouping: true,\n    enableStickyFooter: true,\n    enableExport: true,\n    exportFileName: \"users\",\n    enableEditing: true,\n    editDisplayMode: \"row\",\n    defaultShowColumnFilters: true,\n    initialState: { pagination: { pageSize: 10 } },\n    onSaveRow: ({ row, values, exit }) => {\n      setData((prev) =>\n        prev.map((r) =>\n          r.id === row.id ? { ...r, ...(values as Partial<User>) } : r\n        )\n      )\n      exit()\n    },\n    renderRowActions: ({ row }) => (\n      <Button\n        variant=\"ghost\"\n        size=\"icon-xs\"\n        aria-label=\"Delete row\"\n        onClick={() =>\n          setData((prev) => prev.filter((r) => r.id !== row.original.id))\n        }\n      >\n        <RiDeleteBinLine />\n      </Button>\n    ),\n  })\n  return <DataTable table={table} />\n}"
}
